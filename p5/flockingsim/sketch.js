function setup() {

  createCanvas(windowWidth, windowHeight, WEBGL);
  easycam = createEasyCam();

}

function draw(){
  background(64);
  // fill(255);
  noFill();
  sphere(200);
  // print("hi");
  // print(frameRate());
}